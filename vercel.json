{
  "version": 2,
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "command": "npm run build",
    "outputDirectory": "build"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
